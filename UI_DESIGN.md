# MiMo-OPT 终端界面视觉设计

## 设计理念

**一句话：面向程序员的终端 AI 编码助手 —— 不只是聊天 App，更是能读写项目文件、执行 shell 命令、缓存感知省钱的编程伙伴。**

- 不做 IDE 那种密密麻麻的多面板，但保留编码场景的核心能力（文件操作、shell 集成、代码块操作）
- 不做纯文字没有层次的简陋工具，代码块有语法高亮和语言标签，对话有角色区分
- 做一个**干净、有呼吸感、看着舒服、但干活利索**的ai终端与桌面工作界面

---

## 整体效果

### 默认界面（无侧边栏）

```
 ╭─ MiMo-OPT ────────────────────────────── ⚡ mimo-v2-flash ─╮
 ╰──────────────────────────────────────────────────────────────╯

   你好，帮我写一个 Rust 的 HTTP 服务器


   好的，我来帮你实现一个基于 tokio 的异步 HTTP 服务器：

   ┌─ rust ──────────────────────────────────────────────────┐
   │                                                          │
   │  use tokio::net::TcpListener;                           │
   │  use tokio::io::{AsyncReadExt, AsyncWriteExt};          │
   │                                                          │
   │  #[tokio::main]                                         │
   │  async fn main() -> Result<(), Box<dyn std::error::     │
   │      Error>> {                                          │
   │      let listener = TcpListener::bind("127.0.0.1:3030") │
   │          .await?;                                        │
   │      println!("Listening on port 3030");                │
   │                                                          │
   │      loop {                                              │
   │          let (mut socket, _) = listener.accept().await?; │
   │          tokio::spawn(async move {                       │
   │              handle_connection(&mut socket).await;       │
   │          });                                             │
   │      }                                                   │
   │  }                                                       │
   │                                                          │
   └──────────────────────────────────────────────────────────┘

   然后实现处理函数：

   ┌─ rust ──────────────────────────────────────────────────┐
   │  async fn handle_connection(socket: &mut tokio::net::   │
   │      TcpStream) {                                       │
   │      let mut buf = [0; 1024];                           │
   │      socket.read(&mut buf).await.unwrap();              │
   │                                                          │
   │      let response = "HTTP/1.1 200 OK\r\n\r\nHello!";   │
   │      socket.write_all(response.as_bytes()).await        │
   │          .unwrap();                                     │
   │  }                                                       │
   └──────────────────────────────────────────────────────────┘

   你可以用 cargo run 启动，然后 curl http://localhost:3030 测试。


 ──────────────────────────────────────────────────────────────

   › 输入消息...                                   Ctrl+Enter ↵

 ╭──────────────────────────────────────────────────────────────╮
 │  ◉ 3.2K tok   ▸ 98.1% hit   ￥0.04    mimo-v2-flash        │
 ╰──────────────────────────────────────────────────────────────╯
```

### 流式生成中（打字机效果）

```
   好的，我来帮你实现一个基于 tokio 的异步 HTTP 服务器：

   ┌─ rust ──────────────────────────────────────────────────┐
   │  use tokio::net::TcpListener;▎                          │  ← 光标闪烁
   └──────────────────────────────────────────────────────────┘

 ╭──────────────────────────────────────────────────────────────╮
 │  ◉ 1.1K tok   ▸ 98.1% hit   ￥0.02    ◌ generating...     │  ← spinner
 ╰──────────────────────────────────────────────────────────────╯
```

### 多轮对话

```
 ╭─ MiMo-OPT ────────────────────────────── ⚡ mimo-v2-flash ─╮
 ╰──────────────────────────────────────────────────────────────╯

   解释一下上面代码中 tokio::spawn 的作用


   tokio::spawn 用于创建一个新的异步任务，它会：

   • 在 tokio 的线程池中并发执行
   • 不阻塞当前函数，立即返回一个 JoinHandle
   • 任务完成后可以通过 .await 获取结果

   在 HTTP 服务器的场景中，它的作用是：

   ┌─ text ──────────────────────────────────────────────────┐
   │  每个连接 → 一个独立的异步任务 → 并发处理               │
   │                                                          │
   │  主循环不等待，立刻接受下一个连接                         │
   └──────────────────────────────────────────────────────────┘

   这就是 tokio 实现高并发的核心方式。

 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─

   你好，帮我写一个 Rust 的 HTTP 服务器


   好的，我来帮你实现一个基于 tokio 的异步 HTTP 服务器：

   ┌─ rust ──────────────────────────────────────────────────┐
   │  use tokio::net::TcpListener;                           │
   │  ...                                                    │
   └──────────────────────────────────────────────────────────┘


 ──────────────────────────────────────────────────────────────

   ›                                                   ↵

 ╭──────────────────────────────────────────────────────────────╮
 │  ◉ 8.7K tok   ▸ 96.3% hit   ￥0.11    mimo-v2-flash        │
 ╰──────────────────────────────────────────────────────────────╯
```

### 侧边栏（Ctrl+B 呼出）

```
 ╭─ MiMo-OPT ────────────────────────────── ⚡ mimo-v2-flash ─╮
 ╰──────────────────────────────────────────────────────────────╯

 ┌─ sessions ──────┐
 │                  │
 │ ▸ Rust HTTP 服务器   ← 当前会话，亮色
 │   解释 spawn 作用
 │                  │
 │   调试 API 超时
 │   部署脚本优化
 │   代码审查
 │                  │
 │ ─────── + ─────  │  ← 新建会话
 │                  │
 └──────────────────┘

   好的，我来帮你实现...
```

---

## 配色方案

基于 **Tokyo Night** 色系，终端友好：

| 元素 | 颜色 | 色值 | 说明 |
|------|------|------|------|
| 背景 | 终端默认 | - | 透明，跟随终端主题 |
| 标题栏边框 | 深蓝紫 | #565f89 | 淡雅，不抢眼 |
| 标题文字 | 亮紫 | #7aa2f7 | MiMo-OPT 品牌色 |
| 模型标签 | 青色 | #7dcfff | 柔和的亮色点缀 |
| 用户消息 | 亮白 | #c0caf5 | 正常阅读亮度 |
| MiMo 回复 | 浅灰白 | #a9b1d6 | 比用户消息稍暗，区分角色 |
| 代码块边框 | 深灰 | #3b4261 | 存在感低，不干扰代码 |
| 代码块背景 | 深色 | #1a1b26 | 经典深色代码背景 |
| 代码关键字 | 紫色 | #bb9af7 | 语法高亮 |
| 代码字符串 | 绿色 | #9ece6a | 语法高亮 |
| 代码函数 | 蓝色 | #7aa2f7 | 语法高亮 |
| 代码注释 | 灰色 | #565f89 | 弱化 |
| 引用块边框 | 深蓝 | #3d59a1 | 有层次感 |
| 状态栏边框 | 深灰 | #3b4261 | 低调 |
| 状态栏数据 | 青色 | #7dcfff | 关键数据亮色 |
| 状态栏标签 | 暗灰 | #565f89 | 弱化标签 |
| 分隔线 | 深灰虚线 | #3b4261 | 存在感低 |
| 输入提示符 | 紫色 | #bb9af7 | 醒目但柔和 |
| spinner | 青色 | #7dcfff | 动态元素亮色 |
| 错误 | 暗红 | #f7768e | 不刺眼的红色 |
| 成功 | 绿色 | #9ece6a | 正向反馈 |
| 选中会话 | 蓝色 | #7aa2f7 | 高亮当前项 |
| 侧边栏标题 | 深灰 | #565f89 | 低调 |

---

## 字符选择

### 边框

```
标题栏：  ╭─ ... ─╮   ╰─ ... ─╯    （圆角，柔和）
代码块：  ┌─ ... ─┐   └─ ... ─┘    （直角，规整）
侧边栏：  ┌─ ... ─┐   └─ ... ─┘    （直角）
状态栏：  ╭─ ... ─╮   ╰─ ... ─╯    （圆角，与标题栏呼应）
分隔线：  ─ ─ ─ ─ ─                 （虚线，弱分隔）
实分隔：  ─────────                  （实线，强分隔）
```

### 图标/符号

```
用户消息前缀：  （无前缀，纯缩进，用颜色区分角色）
MiMo 前缀：    （无前缀，颜色比用户稍暗）
代码块语言：   rust / python / text 写在边框上 ┌─ rust ──
工具执行：     ⚙
完成：         ✓
失败：         ✗
当前会话：     ▸
普通会话：     （空白）
新建会话：     +
spinner：      ⠋ ⠙ ⠹ ⠸ ⠼ ⠴ ⠦ ⠧ ⠇ ⠏
token 图标：   ◉
命中率：       ▸
费用：         ￥
生成中：       ◌
```

---

## 排版细节

### 缩进

- 对话区左右各 3 空格缩进
- 代码块整体再缩进 1 空格（即左边距 4）
- 状态栏左右各 2 空格
- 标题栏左右各 1 空格

### 间距

- 用户消息和 MiMo 回复之间：1 行空行
- MiMo 回复中的段落间：1 行空行
- 代码块前后：1 行空行
- 不同轮次对话之间：用虚线分隔线隔开
- 输入框上方：1 行空行（呼吸感）

### 文字

- 用户输入和 MiMo 回复都不加粗，用颜色区分
- 代码块内文字等宽字体
- 状态栏数据部分可以加粗
- 错误信息用斜体（如果终端支持）

---

## 交互状态

### 正常输入状态

- 输入框有 `›` 提示符，光标在提示符后闪烁
- 底部状态栏显示当前 token 用量和模型

### 生成中状态

- 输入框变为只读，显示 "generating... Esc to stop"
- 底部 spinner 旋转
- token 数实时更新

### 按 Esc 中断

- 立即停止生成
- 已输出的内容保留
- 输入框恢复可编辑状态
- 状态栏显示 "stopped"

### 错误状态

- 状态栏短暂变红（0.5s），显示错误原因
- 自动重试时显示 "retrying (2/3)..."
- 输入内容不丢失

### Ctrl+B 切换侧边栏

- 侧边栏滑入/滑出
- 对话区自动缩窄/展宽
- 无动画，即时切换

---

## 终端适配

### 最小尺寸

- 宽度：60 字符（低于此显示警告）
- 高度：20 行（低于此显示警告）

### 理想尺寸

- 宽度：100-120 字符
- 高度：40+ 行

### 全屏利用

- 代码块自动适应终端宽度换行
- 长消息自动折行
- 历史消息可滚动（PageUp/PageDown）

---

## 对比参考

### 像什么

- **ChatGPT 桌面版**的简洁感 — 消息卡片干净
- **GitHub Copilot Chat**的代码块 — 代码展示漂亮
- **Notion**的留白 — 有呼吸感，不拥挤
- **Spotify TUI**的配色 — 深色背景 + 彩色点缀

### 不像什么

- **不**像 htop 那种密密麻麻的网格
- **不**像 vim 那种全键盘无引导的工具
- **不**像基础命令行那种纯黑白无层次
- **不**像花哨的 neofetch 那种过度装饰
