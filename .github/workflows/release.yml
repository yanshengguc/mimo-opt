name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            ext: .exe
            archive: zip
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            ext: ''
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            ext: ''
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-latest
            ext: ''
            archive: tar.gz

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package (zip)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          Compress-Archive -Path "target/${{ matrix.target }}/release/mimo-opt${{ matrix.ext }}" -DestinationPath "mimo-opt-${{ matrix.target }}.zip"

      - name: Package (tar.gz)
        if: matrix.archive == 'tar.gz'
        run: |
          tar czf "mimo-opt-${{ matrix.target }}.tar.gz" -C "target/${{ matrix.target }}/release/" "mimo-opt${{ matrix.ext }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mimo-opt-${{ matrix.target }}
          path: mimo-opt-${{ matrix.target }}.*

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: MiMo-OPT ${{ github.ref_name }}
          draft: false
          prerelease: true
          generate_release_notes: true
          files: artifacts/*
